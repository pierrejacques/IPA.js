export default {
    condition: template => Object.prototype.toString.call(template) === '[object Array]',
    check(data, template, cb) {

    },
    guarantee(data, template, cb) {

    },
    mock(config, template, cb) {

    }
}
